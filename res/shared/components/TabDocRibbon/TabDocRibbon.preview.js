// TabDocRibbon.preview.js — CSF-style stories for sciter-devtools preview / storybook.
// Story width pinned to the design frame (1920dip); the component itself is width-fluid.
import { TabDocRibbon } from "./TabDocRibbon.js";

export const Default = () => (
  <div style="width: 1920dip">
    <TabDocRibbon />
  </div>
);
